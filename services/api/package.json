{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@acme/api",
  "version": "0.0.0",
  "private": true,
  "license": "COPYRIGHT",
  "scripts": {
    "dev": "bun run --hot ./src/http/server.ts",
    "build": "bun build --compile ./src/http/server.ts"
  },
  "dependencies": {
    "hono": "~3.12.7"
  },
  "devDependencies": {
    "@acme/typescript-config": "workspace:*",
    "@types/bun": "~1.0.0"
  }
}
