{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["./**/.env.*local"],
  "pipeline": {
    "dev": {
      "cache": false
    },
    "lint": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["./dist/**"]
    }
  }
}
