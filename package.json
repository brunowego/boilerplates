{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "license": "COPYRIGHT",
  "scripts": {
    "compose:up": "docker compose up -d",
    "compose:status": "docker compose ps",
    "compose:down": "docker compose down",
    "db:schema:create": "turbo db:schema:create",
    "db:migrate:up": "turbo db:migrate:up",
    "clean": "turbo clean",
    "lint": "trunk check",
    "format": "trunk fmt",
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start"
  },
  "dependencies": {
    "turbo": "~1.13.2"
  },
  "devDependencies": {
    "@acme/typescript-config": "workspace:~",
    "@biomejs/biome": "~1.6.4",
    "@trunkio/launcher": "~1.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@8.15.6",
  "volta": {
    "node": "20.12.1"
  }
}
